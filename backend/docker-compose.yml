version: '3.8'
services:
  api:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      - DATABASE_URL="file:./dev.db"
      - JWT_SECRET="sua_chave_secreta_aqui"